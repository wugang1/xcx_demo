<!--pages/user/user.wxml-->
<view class="body">
  <view class="fl">
    <view class="flbox" wx:for="{{tp}}" wx:key="index" bindtap="tzfq" data-key="{{index}}">
        <image  src='{{item.imgsrc}}'></image>
       <text> {{item.catename}}</text>
    </view> 
  </view>   
</view>